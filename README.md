# Webpack 4 HTML + SCSS + TS starter

Frontend starter: HTML + SCSS + TS + TSLint

## Installation

```bash
npm install
```

## Usage

`npm run dev` or `npm start` - development build

`npm run build` - production build
